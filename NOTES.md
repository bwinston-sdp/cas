to build:
./gradlew build --parallel -x test -x javadoc -x check --build-cache --configure-on-demand 

to run:
./gradlew webapp:cas-server-webapp-native:bootRun --parallel -x test -x javadoc -x check --offline --configure-on-demand --build-cache --stacktrace


need to get an additional button on the login form (login providers?)
